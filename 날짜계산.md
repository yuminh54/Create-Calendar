```
else if (p === 0) {
                for (var i=1; i <= (q-1); i++) {
                  var nextWeek = new Date((Date.parse(next) + (7-day) * 1000 * 60 * 60 * 24))
                  if (nextWeek.getMonth()+1 < 10) {
                    var nextday = 0 + (nextWeek.getMonth()+1) + '-' + nextWeek.getDate() + '-' + nextWeek.getFullYear()
                    totalday.push([nextday, 7])
                  } else {
                    var nextday = (nextWeek.getMonth()+1) + '-' + nextWeek.getDate() + '-' + nextWeek.getFullYear()
                    totalday.push([nextday, 7])
                  }
                }
              } 
```

```javascript
            if (day + (dayCnt - 1) > 7){
              // 첫 주에 표시해야하는 값
              var cnt = 7 - day
              totalday.push([data.start_date, cnt])

              // 표시해야하는 전체 날짜
              var total = day + dayCnt

              // 몫, 나머지
              var q = Math.floor(total / 7)
              var p = total % 7
              if (q > 0){
                if (q == 1) {
                  if (nextWeek.getMonth()+1 < 10) {
                    var nextday = 0 + (nextWeek.getMonth()+1) + '-' + nextWeek.getDate() + '-' + nextWeek.getFullYear()
                    totalday.push([nextday, total - cnt])
                  }
                  else {
                    var nextday = (nextWeek.getMonth()+1) + '-' + nextWeek.getDate() + '-' + nextWeek.getFullYear()
                    totalday.push([nextday, total - cnt])
                  }
                } 
              }
              else if (p === 0) {
                for (var i=1; i <= (q-1); i++) {
                  var nextWeek = new Date((Date.parse(next) + (7-day) * 1000 * 60 * 60 * 24))
                  if (nextWeek.getMonth()+1 < 10) {
                    var nextday = 0 + (nextWeek.getMonth()+1) + '-' + nextWeek.getDate() + '-' + nextWeek.getFullYear()
                    totalday.push([nextday, 7])
                  } else {
                    var nextday = (nextWeek.getMonth()+1) + '-' + nextWeek.getDate() + '-' + nextWeek.getFullYear()
                    totalday.push([nextday, 7])
                  }
                }
              } 
              else {
                for (var i=1; i <= q; i++) {
                  if (i !== q) {
                    var nextWeek = new Date((Date.parse(next) + (7-day) * 1000 * 60 * 60 * 24))
                    if (nextWeek.getMonth()+1 < 10) {
                      var nextday = 0 + (nextWeek.getMonth()+1) + '-' + nextWeek.getDate() + '-' + nextWeek.getFullYear()
                      totalday.push([nextday, 7])
                    } else {
                      var nextday = (nextWeek.getMonth()+1) + '-' + nextWeek.getDate() + '-' + nextWeek.getFullYear()
                      totalday.push([nextday, 7])
                    }
                  } else {
                    var nextWeek = new Date((Date.parse(next) + (7-day) * 1000 * 60 * 60 * 24))
                    var tempDay = total - ((7*(q-1)) + cnt)
                    if (nextWeek.getMonth()+1 < 10) {
                      var nextday = 0 + (nextWeek.getMonth()+1) + '-' + nextWeek.getDate() + '-' + nextWeek.getFullYear()
                      totalday.push([nextday, tempDay])
                    } else {
                      var nextday = (nextWeek.getMonth()+1) + '-' + nextWeek.getDate() + '-' + nextWeek.getFullYear()
                      totalday.push([nextday, tempDay])
                    }
                  }
                }
              }
            }
            data['day'] = day
            data['dayCnt'] = totalday
            schedule.push(data)
```

